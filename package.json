{
  "name": "xdev-bio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "refresh": "node scripts/refresh-cache.mjs",
    "deploy": "astro build && gh-pages -d dist"
  },
  "dependencies": {
    "astro": "^7.1.1"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "gh-pages": "^6.3.0"
  }
}
