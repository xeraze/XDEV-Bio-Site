---
import translations from '../lib/translations';

interface Props {
  locale?: string;
}

const { locale = 'en' } = Astro.props;
const t = (translations[locale as keyof typeof translations] ?? translations.en).contact;

const links = [
  { label: 'Telegram', href: 'https://t.me/xrzeoff', icon: 'telegram' },
  { label: 'Steam', href: 'https://steamcommunity.com/id/xeraze_/', icon: 'steam' },
  { label: 'Email', href: 'mailto:xeraze.official@gmail.com', icon: 'email' },
];
---

<section id="contact">
  <div class="container">
    <p class="eyebrow">{t.eyebrow}</p>
    <h2 class="section-title">{t.title}</h2>
    <p class="section-sub">
      {t.subtitle}
    </p>

    <div class="contact-grid">
      {links.map((link) => (
        <a class="contact-link" href={link.href} target="_blank" rel="noopener">
          <span class="contact-label">{link.label}</span>
          <span class="contact-arrow">↗</span>
        </a>
      ))}

      <button class="contact-link contact-link--btn" id="copy-discord">
        <span class="contact-label">{t.discord}</span>
        <span class="contact-arrow" id="copy-icon">⧉</span>
      </button>
    </div>

    <a href="https://xeraze.github.io/Pricelist/" target="_blank" rel="noopener" class="pricelist-cta">
      {t.pricelist}
    </a>
  </div>
</section>

<div class="toast" id="toast">{t.copied}</div>

<style>
  .section-title {
    font-family: var(--font-display);
    font-size: clamp(1.6rem, 5vw, 2.2rem);
    font-weight: 600;
    margin: 10px 0 12px;
  }

  .section-sub {
    color: var(--ink-2);
    font-size: 0.88rem;
    margin-bottom: 32px;
  }

  .contact-grid {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-bottom: 28px;
  }

  .contact-link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: var(--bg-1);
    border: 1px solid var(--line);
    border-radius: var(--radius-md);
    padding: 16px 20px;
    text-decoration: none;
    color: var(--ink-0);
    font-family: var(--font-mono);
    font-size: 0.92rem;
    font-weight: 500;
    cursor: pointer;
    transition: border-color 0.2s var(--ease), transform 0.2s var(--ease);
    width: 100%;
    text-align: left;
  }

  .contact-link:hover {
    border-color: var(--amber-dim);
    transform: translateX(4px);
  }

  .contact-arrow {
    color: var(--ink-2);
  }

  .pricelist-cta {
    display: inline-block;
    color: var(--amber);
    font-size: 0.9rem;
    text-decoration: none;
    font-weight: 600;
    margin-bottom: 64px;
  }

  .pricelist-cta:hover {
    text-decoration: underline;
  }

  .toast {
    position: fixed;
    bottom: 28px;
    left: 50%;
    transform: translateX(-50%) translateY(20px);
    background: var(--bg-2);
    border: 1px solid var(--line-strong);
    color: var(--ink-0);
    font-family: var(--font-mono);
    font-size: 0.85rem;
    padding: 12px 20px;
    border-radius: var(--radius-sm);
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s var(--ease), transform 0.3s var(--ease);
    z-index: 200;
  }

  .toast.show {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
  }
</style>

<script>
  const btn = document.getElementById('copy-discord');
  const toast = document.getElementById('toast');

  btn?.addEventListener('click', async () => {
    try {
      await navigator.clipboard.writeText('xeraze.');
    } catch {
    }
    toast?.classList.add('show');
    setTimeout(() => toast?.classList.remove('show'), 2000);
  });
</script>